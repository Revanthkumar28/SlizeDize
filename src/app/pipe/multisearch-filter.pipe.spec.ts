import { MultisearchFilterPipe } from './multisearch-filter.pipe';

describe('MultisearchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MultisearchFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
