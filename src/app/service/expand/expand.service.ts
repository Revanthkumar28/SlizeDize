import { Injectable } from '@angular/core';
import { MenuDropdownComponent } from 'src/app/material-designs/menu-dropdown/menu-dropdown.component';

@Injectable({
  providedIn: 'root'
})
export class ExpandService {

  constructor() { }


  // addDropdown(dropdown: MenuDropdownComponent) {
  //   this.amazeCart.push(dropdown);
  // }

  // closeOtherDropdowns(openDropdown: MenuDropdownComponent) {
  //   this.amazeCart.forEach((dropdown:any) => {
  //     if (dropdown !== openDropdown) {
  //       dropdown.closeDropdown();
  //     }
    
  // }
}
