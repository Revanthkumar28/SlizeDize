import { UpArrowKeyDirective } from './up-arrow-key.directive';

describe('UpArrowKeyDirective', () => {
  it('should create an instance', () => {
    const directive = new UpArrowKeyDirective();
    expect(directive).toBeTruthy();
  });
});
