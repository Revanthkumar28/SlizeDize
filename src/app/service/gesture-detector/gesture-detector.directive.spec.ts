import { GestureDetectorDirective } from './gesture-detector.directive';

describe('GestureDetectorDirective', () => {
  it('should create an instance', () => {
    const directive = new GestureDetectorDirective();
    expect(directive).toBeTruthy();
  });
});
